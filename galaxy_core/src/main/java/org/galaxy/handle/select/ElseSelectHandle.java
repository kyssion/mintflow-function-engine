package org.galaxy.handle.select;

import org.galaxy.param.ParamWrapper;

public class ElseSelectHandle extends SelectorHandle {
    @Override
    public boolean select(ParamWrapper p) {
        return true;
    }
}
