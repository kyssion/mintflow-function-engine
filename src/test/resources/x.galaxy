namespace(namespace1){
    process(process1)->if(sone){
        ->h(one)->r(rone){one,one,one}
    }elif(sone){
        ->h(two)->r(rone){two,two,two}
    }el{
        ->if(sone){
            ->r(rone){two,two,two}
        }elif(sone){
            ->r(rone){two,two,two}
        }->r(rone){two,two,two}
    };
}

#A = namespaceId
#B = processid
#C = handleid
#D = handleIdList
#Z = SZ|#
#S = namespace(A){K}
#K = process(B)P;K|#
#P = ->h(C)P|->if(xxx){P}E el(){}|->r(c){handleIdList}P|#
#E = ->elif(c){P}E|#